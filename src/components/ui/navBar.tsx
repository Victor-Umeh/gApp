import { Android, Apple } from "@/icons";

const NavBar = () => {
  return (
    <nav>
      {/* logo */}
      {/* nav-links */}
      {/* hamburger */}
      {/* nav-menu */}
      navBar
    </nav>
  );
};

export default NavBar;
